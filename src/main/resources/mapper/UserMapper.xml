<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ca.sait.backup.mapper.UserMapper">


    <insert id="create" parameterType="User">

        INSERT  INTO user (name, pwd, head_img, phone , create_time,email)
        values (#{name,jdbcType=VARCHAR}, #{pwd,jdbcType=VARCHAR}, #{headImg,jdbcType=VARCHAR},
        #{phone,jdbcType=VARCHAR},#{createTime,jdbcType=TIMESTAMP},#{email,jdbcType = VARCHAR})

    </insert>

    <update id="update" parameterType="User">
        UPDATE user SET name = #{name,jdbcType=VARCHAR},pwd = #{pwd,jdbcType=VARCHAR},head_img = #{headImg,jdbcType=VARCHAR},
        phone = #{phone,jdbcType=VARCHAR},create_time = #{createTime,jdbcType=TIMESTAMP},email = #{email,jdbcType = VARCHAR}
        Where id = #{id}
    </update>

    <!--根据手机号查询用户信息-->
    <select id="findByPhone" resultType="User">

        select  * from user where phone =#{phone}

    </select>


    <!--根据手机号和密码找用户-->
    <select id="findByEmailAndPwd" resultType="User">

        select  * from user where email =#{email} and pwd = #{pwd}


    </select>


    <select id="findByUserId" resultType="User">

      select  * from user where id=#{user_id}

    </select>

</mapper>